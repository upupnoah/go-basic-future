package main

/*
const (
	GoSlogan = "less is more"
	s1       = "hello, gopher"
)
var s2 = "I love go"
*/

func main() {

	/*
		var s string = "hello"
		s[0] = 'k' //error：字符串的内容是不可变的
		s = "gopher" // 重新赋值是ok的
	*/

	/*
		var s = "中国人"
		fmt.Printf("the length of s = %d\n", len(s)) //9
		for i := 0; i < len(s); i++ {
			fmt.Printf("0x%x ", s[i])
		}
		fmt.Printf("\n")
	*/

	/*
		var s = "中国人"
		fmt.Println("the character count in s is", utf8.RuneCountInString(s)) // 3
		for _, c := range s {
			fmt.Printf("0x%x ", c) // 0x4e2d 0x56fd 0x4eba
		}
		fmt.Printf("\n")
	*/

}
